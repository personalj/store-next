import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Home page',
  description: 'Home page description',
};
const Home = () => {
  return <div>Home</div>;
};

export default Home;
